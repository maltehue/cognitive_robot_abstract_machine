"""
Synthesis of pluggable symbolic theories from natural-language instructions.

An instruction sentence goes in; a compiled theory comes out, or a typed rejection
naming what to fix. The language model only ever produces a specification — the
validated data format — so everything that must be right for the controller is checked
before anything runs.
"""
